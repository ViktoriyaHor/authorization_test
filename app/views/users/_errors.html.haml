- if @user && @user.errors.any?
  #error_explanation
    .alert.alert-danger
      %button{:class => 'close', "data-dismiss" => 'alert'} &times;
      %h5.text-center The form contains #{pluralize(@user.errors.count, "error")}.
      %ul
        - @user.errors.full_messages.each do |msg|
          %li= msg
